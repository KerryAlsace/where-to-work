<h1>Edit <%= @user.username %>'s Username or Role</h1>

<%= form_for @user do |f| %>
  
  <%= f.label :username %>:
  <%= f.text_field :username %>

  <%= f.label :role, "Admin" %>
  <%= f.check_box :role %>

  <%= f.submit "Save Edits" %>
<% end %>